#!/usr/bin/env bash
# This script displays a list of currently running processes.

# Use the ps command to display all running processes, with a tree-like format.
ps -eo pid,ppid,user,%cpu,%mem,cmd --forest

